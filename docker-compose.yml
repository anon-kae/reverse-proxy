version: "3.8"
services:
  proxy:
    image: nginx:1.19-alpine
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./ssl:/etc/nginx/ssl
    restart: always
  app:
    image: web_api_smart_locker
    ports:
      - "5000:5000"